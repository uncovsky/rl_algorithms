import numpy as np
import torch

"""
    Layer initialization for policy gradient methods.
"""
def initialize_layer(layer, std=np.sqrt(2), bias_const=0.0):
    torch.nn.init.orthogonal_(layer.weight, std)
    torch.nn.init.constant_(layer.bias, bias_const)
    return layer
